# DrDoc

DrDoc screens uploaded PDF files.

If a PDF contains fishy content (for example JavaScript actions, launch actions,
embedded files, suspicious command markers, or URI actions), DrDoc adds that
upload request to a request queue for follow-up.
